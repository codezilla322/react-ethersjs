import "./App.css";

function App() {
  const handleSend = () => {};

  return (
    <>
      <div className="card">
        <button onClick={() => handleSend()}>Send</button>
      </div>
    </>
  );
}

export default App;
